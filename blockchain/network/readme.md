If you contruct blockchain network for the first time, input following command.

> bash test_network_init.sh

When you run the blockchain network, select one of them below.

1. interact with geth cilent in http protocol. No need to mine manually.
> bash test_network_start_http_automine.sh

2. interact with geth cilent in http protocol.
> bash test_network_start_http.sh

@depreated
3. just run geth cilent.
> bash test_network_start_local.sh
